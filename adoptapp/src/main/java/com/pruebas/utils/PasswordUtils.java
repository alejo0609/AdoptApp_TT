//* ESTA ES UNA OPCION PARA GENERAR CLAVE ENCRIPTADAS *//

/*
package com.pruebas.utils;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

public class PasswordUtils {
    private static final BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();

    public static String encriptarPassword(String password) {
        return encoder.encode(password);
    }

    public static void main(String[] args) {
        // Generar una contraseña encriptada y mostrarla en consola
        String password = "123456"; // 🔹 Cambia esto por la contraseña deseada
        String passwordEncriptada = encriptarPassword(password);
        System.out.println("Contraseña encriptada: " + passwordEncriptada);
    }
}
*/